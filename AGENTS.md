# AGENTS.md（给 Codex 及其他代理）

本仓库是一组“技能”：每个 `skills/<name>/SKILL.md` 描述一种任务的工作法，同目录下的 `references/`、`assets/`、`scripts/`、`examples/` 是它需要的文件。用户要写中文深度报告、口号、核查材料、销售问答或排版 Word 时，先读对应技能的 SKILL.md，按它的步骤做。

- 不知道用哪个：研究/营销方案 → `promotion-master`，先推荐完整/部分/现稿优化与方向，确认后执行；单模块 → 对应卫星。已有确认不重问。
- 默认安装仅选择自包含 `promotion-master`；`--all` 才安装四个卫星。已安装旧 report-skills 时不得覆盖其链接；只写文本不强求Word工具。实际构建前doctor检查，缺依赖不当作通过。
- 每个数字带来源与日期；材料内部口径矛盾单独列出；不写“唯一”“首只”“稳赚”一类慎用词（见 `shared/references/caution-words.md`）。
- 没有联网检索能力时，把需要检索的项写进数据缺口，不要用记忆里的数字冒充查证结果。
- 不能查看图片时，`render_qa.py` 生成的缩略图交给用户看，并在交付说明里写明“版式未经代理目视检查”。
- 只改任务需要的文件；共用文件改 `shared/` 再运行 `python3 tools/sync_shared.py`。
- 真实附件和客户资料不进公共仓库。方向、原句保留和客户偏好属于项目，不自动写成通用规则。
- 自检输出只在临时目录；不得清空用户传入目录。脚本 QA 与语义评审、模拟与真实用户试用分开报告。
- 完整推广报告默认核心判断、定位、十二问十二答与一分钟话术；其他研究/局部任务不强套。判断提炼见insight-discovery，成熟示范见insight-examples，纠错渲染例不代替成熟示范。
- 本项目仍是预览分支，先交实际测试成品供使用者验收；未经确认不创建正式Release、不替换本机旧技能。原repo和真实内部附件不修改或上传。
