# 给 Codex 及其他代理的项目约定

Promotion Master 当前正式版本为 1.0.0，默认分支为 `main`。
每个 `skills/<name>/SKILL.md` 描述一种任务工作法，相关 `references/`、`assets/`、
`scripts/` 和 `examples/` 为该技能所需资源。执行任务前先读对应技能。

- 研究与推广任务使用自包含的 `promotion-master`；定位、问答、核查或 Word 单项任务可使用相应专项技能。按已确认范围执行，已有授权不重复问。
- 按最终读者明确内部版、客户版或两版配套。客户正文与制作人说明分离，保留证据与承重条件；两版分别交付并核对一致性。未知用途先推荐，不默认内部版。
- 完整产品推广报告默认包含核心判断、定位、十二问和产品要点；指定三问、单句或其他局部任务不扩成全套。判断方法见 `insight-discovery`，成熟写法见 `insight-examples`。
- 默认安装只准备文字能力，`--with-word` 才准备排版依赖，`--all` 才增加四个专项入口。安装目标冲突时停止，不覆盖已有目录或其他链接。实际 Word 构建前运行 doctor，缺依赖不当作通过。
- 每个数字带来源、日期与口径；材料矛盾单列。遵守用户的数据范围，无法检索时列缺口，不用记忆补数。慎用词见 `shared/references/caution-words.md`。
- 客户稿只使用公开或明确获准披露的依据；生成不等于发送、上传、发布或合规批准。
- 共用规则与脚本只在 `shared/` 修改，再运行 `python3 tools/sync_shared.py`；独立技能资源必须随包完整。只修改任务需要的文件。
- 真实附件、客户资料、凭据和私有评审材料不进公共仓库。客户偏好与指定原句属于具体项目，不自动变成通用规则。
- 自检输出只写临时目录，不清空用户材料目录。不能看渲染图时，如实说明“版式未经代理目视检查”。
- 原始回复、作者自检和后续独立评审分别保存；维护者回放不称新会话盲测。脚本检查、内容审阅与真实使用者试用分别报告，未经执行不预写通过结果。
- 正式验证记录见 `docs/validation-v1.0.0.md`；平台结论不得超出 macOS 本机与 Linux CI 中实际执行的范围。发布或真实安装仍须符合当次用户授权。
