# Codex 实测清单

在本机按顺序做，每项记录结果与日期。

| # | 检查 | 命令或操作 | 通过标准 |
|---|---|---|---|
| 1 | 预检 | `python3 shared/scripts/doctor.py` | 必需项全 OK；中文字体找到 PingFang 或 Noto |
| 2 | 依赖 | `tools/install_codex.sh 临时技能目录` 后 `tools/selftest.sh` | shared及实际技能入口均能解析依赖，取消 NODE_PATH 仍可构建；缺工具不静默跳过 |
| 3 | 安装 | 临时目录执行默认安装；另一个临时目录执行 `--all` | 默认仅主技能，全量五个；旧链接不被覆盖。真实Codex列表需另经用户确认安装后检查 |
| 4 | 触发 | 显式调用 `$promotion-master` 做所选模块 | 读取本包，旧版卫星不覆盖新规则；默认安装无需卫星也能完成 |
| 5 | 闸门 | 说“做一份产品分析与营销报告，先给思路” | 执行前给范围建议、不同叙事小样、取舍与保留项，并停止等确认 |
| 6 | 联网 | 观察第 5 项里外部坐标一节 | 能检索：数字带检索日期；不能检索：写入数据缺口，不用记忆数字 |
| 7 | 看图 | 让它跑 `render_qa.py` 并评价版式 | 能看图：指出具体页的问题；不能看图：明确说明“未目视检查” |
| 8 | 回放 | 用私有金标准的任务与原始材料，或 evals 虚构输入 | 先验证停点，再提供选择并走到最终交付；记录推论、范围、原句、可复述与跨成品一致性 |

`validation-v1.3.md`仅为上游历史；本项目结果另记。没有实际运行的项目不能勾通过；
模拟前向测试不等于真实同事试用，终稿没有原始资料时不宣称完成金标准复现。

记录位置：`evals/replay-log.md`（私有，不提交）。
