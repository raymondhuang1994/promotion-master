# 来源与迁移边界

Promotion Master 是独立项目，不是对原仓库的重命名操作。

- 继承公开仓库 `raymondhuang1994/report-skills` 的 v1.3.0 源码快照，提交 `2c96b4ee016209f791db45ac2d21019cc607fb67`。
- 原始源码作者为同一仓库所有者 Raymond。新仓库不导入私有附件、本地凭据、安装依赖目录或旧仓库的Git历史。
- 旧源码中的CHANGELOG、`docs/validation-v1.3.md`记录上游历史，不能作为本项目新增功能已通过验收的证据。
- 新主入口由 `exec-deep-report` 变为 `promotion-master`，显示名称为 Promotion Master。旧入口保留在原安装，不冒充本项目别名。
- 新项目默认仅安装主入口，专项卫星可显式选择；不覆盖另一个仓库已安装的同名目录或软链接。
- 主入口自身携带研究、定位、问答、排版的必要规则；无需调用旧版卫星完成任务。
- 本轮开发、测试在新目录与临时目录，原仓库和原Codex安装保持不变。正式发布及本机切换等待使用者对测试成品确认。

当前版本为 `0.1.0-preview.2`。本次加入受众版本前的回滚点为 `backup/pre-audience-editions-20260925`（`40c26a2`）；它是开发备份，不是正式发布。保留旧安装意味着退回旧工作法无需破坏新项目：显式调用原 `$exec-deep-report` 即可。
若以后要替换卫星或卸载某版本，应先解析具体链接与目标，再向使用者说明迁移范围；不使用批量删除清空技能目录。
